import { Flex } from "@mantine/core";

export const Footer = () => {
  return (
    <Flex
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: 40,
      }}
    >
      @bronepuh 2024
    </Flex>
  );
};
